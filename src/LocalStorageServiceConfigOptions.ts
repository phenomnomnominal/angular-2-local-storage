'use strict';

import { OpaqueToken } from '@angular/core';

const LOCAL_STORAGE_SERVICE_CONFIG: string = 'LOCAL_STORAGE_SERVICE_CONFIG';
export const LOCAL_STORAGE_SERVICE_CONFIG_OPTIONS = new OpaqueToken(LOCAL_STORAGE_SERVICE_CONFIG);
